# Copy to docker/.env.frontend-build (gitignored) and fill in.
# These OAuth client IDs are baked into the frontend JS bundle at build time.
# Leave blank for integrations you don't use -- the button will just not work.

SLACK_CLIENT_ID=
LINEAR_CLIENT_ID=
GITHUB_CLIENT_ID=
JIRA_CLIENT_ID=
DISCORD_CLIENT_ID=
CLICKUP_CLIENT_ID=
GITLAB_CLIENT_ID=
HEIGHT_CLIENT_ID=
MICROSOFT_TEAMS_BOT_ID=
